<div class="form-wrapper signup-form-wrapper">
  <div class="row">
    <div class="form-container">
      <h3>Sign up</h3>
      <p><em>Discover food. Get organised.</em></p>

      <div class="signup-login-form">
        <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
          <%= f.error_notification %>

          <div class="form-inputs">
            <%= f.input :first_name, required: true, placeholder: "Gordon"%>
            <%= f.input :last_name, required: true, placeholder: "Ramsay" %>
            <%= f.input :email, required: true, autofocus: true, placeholder: "example@example.com" %>
            <%= f.input :password, required: true, placeholder: "••••••••", hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length) %>
            <%= f.input :password_confirmation, required: true, placeholder: "••••••••" %>
            <%= f.input :photo %>
            <%= f.input :photo_cache, as: :hidden %>
          </div>

          <div class="form-actions">
            <%= f.button :submit, "Sign up" %>
          </div>
        <% end %>
      </div>
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
